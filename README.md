# Notype + Flow Mates CMS

Publieke website is Notype (ongewijzigd). Het CMS erachter is Flow Mates (`/cms` of `/admin`).

```bash
npm install
cp .env.example .env.local
npm run dev
```

- Site: http://localhost:5173/
- CMS: http://localhost:5173/cms
